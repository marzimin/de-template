# Dependencies installed inside the Airflow Docker image.
# Mirrors the core runtime deps from pyproject.toml — managed separately
# because Airflow uses pip internally, not uv.
#
# IMPORTANT: Keep versions in sync with pyproject.toml [project.dependencies].
# When adding or updating a package here, update pyproject.toml as well.

apache-airflow-providers-standard>=1.0.0
dbt-postgres>=1.8.0
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.7.0
tenacity>=8.3.0
structlog>=24.0.0
