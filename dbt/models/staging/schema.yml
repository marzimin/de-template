version: 2

models:
  - name: stg_example_items
    description: Starter staging model used to verify dbt runs in a fresh clone.
    columns:
      - name: item_id
        tests:
          - not_null
          - unique

  - name: example_items
    description: Starter mart model used to verify dbt tests in a fresh clone.
    columns:
      - name: item_id
        tests:
          - not_null
          - unique
